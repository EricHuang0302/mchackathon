{
  "name": "firstaid-firestore-rules-tests",
  "version": "0.0.0",
  "private": true,
  "description": "Firestore Security Rules tests for the incident data layer. Requires the Firebase Emulator Suite (and a Java runtime).",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "test:emulator": "firebase emulators:exec --project demo-firstaid --only firestore \"node --test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.1"
  }
}
